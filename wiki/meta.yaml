# Wiki metadata — tracks the last full synchronization of wiki content against source code.
#
# Fields:
#   commit        — full SHA of the HEAD commit at time of update
#   timestamp     — ISO-8601 UTC timestamp of the update
#   branch        — branch from which HEAD was read
#   updated_by    — model or author that performed the update
#   coverage_note — brief summary of what was audited
#
# Usage:
#   Next time a wiki update is requested, run `git log <commit>..HEAD --oneline`
#   to see every commit that landed since the last sync, then update affected pages.

last_update:
  commit: "3be0262c4004c193fcc70a249f4f6743765a1984"
  timestamp: "2026-05-05T06:14:16Z"
  branch: "main"
  updated_by: "codex"
  coverage_note: >
    Synced wiki against the demo tree refactor in the working tree. Added
    src/demos/ pages for core, integrations, product/dashboard, narratives,
    safe_skills, mcpzero, and assets. Updated root/source indexes,
    safe_mcp_proxy/examples wrapper docs, attacks corpus docs, MCPZero docs,
    tests overview, and log.md to reflect that runnable demos now live under
    demos/ while old entry points remain compatibility wrappers.
